{
  "name": "worksite",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "docs:generate": "npx tsx scripts/generate-docs.ts",
    "postinstall": "pnpm docs:generate || true"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2"
  },
  "packageManager": "pnpm@9.15.0"
}
